<template>
    <button v-ripple :class="cx('lastPageButton')" type="button" v-bind="getPTOptions('lastPageButton')" v-on="$listeners" data-pc-group-section="pagebutton">
        <DynamicComponent :template="template || 'AngleDoubleRightIcon'" :className="cx('lastPageIcon')" v-bind="getPTOptions('lastPageIcon')" />
    </button>
</template>

<script>
import BaseComponent from 'primevue2/basecomponent';
import AngleDoubleRightIcon from 'primevue2/icons/angledoubleright';
import Ripple from 'primevue2/ripple';

export default {
    name: 'LastPageLink',
    hostName: 'Paginator',
    extends: BaseComponent,
    props: {
        template: {
            type: Function,
            default: null
        }
    },
    methods: {
        getPTOptions(key) {
            return this.ptm(key, {
                context: {
                    disabled: this.$attrs.disabled
                }
            });
        }
    },
    components: {
        AngleDoubleRightIcon: AngleDoubleRightIcon
    },
    directives: {
        ripple: Ripple
    }
};
</script>
